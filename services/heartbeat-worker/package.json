{
  "name": "@workspace/heartbeat-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "heartbeat:once": "tsx src/once.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "^5.9.3",
    "wrangler": "^4.46.0"
  }
}
